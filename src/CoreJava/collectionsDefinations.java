package CoreJava;

public class collectionsDefinations {
	
	//List--it is an ordered collection(called sequence).List may contain duplicate Elements
	//Below classes implements list interface
	//1) ArrayList ,2)LinkedList 3)Vector
	
	
	//Set -->Set is a collection that cannot contain Duplicate elements
	//below are classes implements set interface
	//1) HashSet, 2)LinkedHashset, 3)TreeSet
	
	
	//Map-->Map is an object that maps keys to values.A map cannot contain duplicate keys.
	//Below are main implementations to map interface
	//1)Hashmap 2)LinkedHashmap 3)Treemap

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
